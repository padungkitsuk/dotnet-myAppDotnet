services:
  api:
    build: .
    ports:
      - "5000:80" 
    environment:
      - ASPNETCORE_URLS=http://+:80
      - ASPNETCORE_ENVIRONMENT=Development
      # แก้ไข Connection String ให้ชี้ไปยัง IP ของเครื่องคุณ (Host IP)
      - ConnectionStrings__DefaultConnection=Server=host.docker.internal;Database=DEV;User Id=sa;Password=P@ssw0rd;TrustServerCertificate=True;